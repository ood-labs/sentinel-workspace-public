# Scientific Organism Proof

This is the curated proof set for the committed example.

| Artifact | What it proves |
| --- | --- |
| `16_final_master.png` | Signed-off 1280x720 final Program still |
| `scientific_organism_motion_final.mp4` | 8.03-second motion master, 478 frames, zero dropped frames |
| `biotic_coordinate_field_emphasis.png` | Raw Biotic Source Field response at the real normalized seed coordinates, without relying on the later amber overlay |
| `seed_lab_toolbar_right.png` | Final follow-panel Seed Lab layout with the tool cluster clear of the title |
| `checkpoint_final/output.png` | Final checkpoint output |
| `checkpoint_final/graph_profile.json` | Live per-node graph profile captured with the final look |

The raw field-coordinate image temporarily emphasized `seed_gain` and
`reaction_rate` during capture, then restored the live values. It includes a
seed below `y = 0.5`, proving that the half-resolution feedback pass covers the
complete normalized stage rather than only its upper-left quarter.

Additional intermediate captures may exist in an authoring checkout, but they
are not part of the committed example.
