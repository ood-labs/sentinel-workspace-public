#ifndef AU_HUD_TEXT_LOCAL
#define AU_HUD_TEXT_LOCAL
// AUTOPSIA — thin forwarder. The typography now lives in the shared AU HUD
// library so every instrument node renders identical type.
#include "../_shared/au_hud/au_text.hlsli"
#endif
