# Phase 94-97 Agent Doc Sync

Progress saved.

Brought this workspace's agent-facing docs and skills up to date with the features shipped in Sentinel phases 94 through 97, sourced from the installer template payload where those phases landed their edits.

- Added `knowledge/ui-interactions.md`, the complete keyboard and mouse reference from Phase 94, extended with the Phase 96 Embedded Terminal settings behavior: matching System and Project tab sections, project-over-system override, and a cleared Launch command opening plain PowerShell.
- Linked the new page from `knowledge/FEATURE-MAP.md`.
- Replaced the stale Managed Instruction Persistence section in `CLAUDE.md` with the Phase 95 Workspace Ownership section: the default workspace mirrors the installed version, a custom workspace path is fully user-owned and seeded once when empty, and the `update-workspace` skill is the user-side update path. `AGENTS.md` and `GEMINI.md` are byte-identical copies.
- Added the `update-workspace` skill (Phase 95) to both skill trees.
- Updated the `sentinel-bug-report` skill and guide (Phase 97): unlicensed submissions ship the narrative and inline diagnostic without a file bundle, receive the `unlicensed` label, and are limited to 3 per machine fingerprint and 10 per IP each day.

The node_examples collection from Phase 94 stays out of this workspace for now pending review; it remains recoverable from the public repo's orphaned publication commit.

This is a documentation refresh checkpoint, not a phase or subphase completion.
