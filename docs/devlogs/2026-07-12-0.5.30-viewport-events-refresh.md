# Workspace refresh for 0.5.30 authored viewport events — 2026-07-12

## Progress saved

- Audited the delta since anchor `52ac446d`: the 0.5.30 release (commit `11c41893`), the first installer carrying Phase 88 authored viewport events. No new MCP tools or pipeline types landed in the range.
- Authored viewport events are now documented as shipped: the agent manuals, `knowledge/module-pipeline.md`, and the `module-authoring` skill state availability on installs at 0.5.30 or newer, replacing the previous "builds newer than 0.5.29" caveats. That pending caveat is resolved and removed from the anchor.
- Recovered a second provisioning regression: 0.5.30 was also built before the sentinel seed edits were committed, so launching it overwrote the workspace docs with old seeds again. The drift was discarded, the current seeds re-copied and verified, and the 0.5.30 version bump kept.

## Still in progress

- Commit the sentinel repo seed edits so the next installer stops regressing these docs on provisioning.

This is a documentation refresh checkpoint, not a phase or subphase completion.
